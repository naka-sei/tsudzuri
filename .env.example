# Example environment (copy to .env and fill values)
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=tsudzuri
DB_SSL_MODE=disable

# Firebase / GCP
#GCP_SA_KEY_JSON_PATH=/path/to/service-account.json
#GCP_PROJECT_ID=your-project-id

# Application
#APP_ORIGIN=http://localhost:3000
